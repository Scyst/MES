# MES Team Planner Sync Board - 2026-08-25

## 🔴 Currently Working On

---

## ✅ Completed

### 👨‍💻 Agent Activity (08:15 - 08:30)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  1. การเปลี่ยนทีมในเมนู Team Settings (HC Group) ส่งผลให้ข้อมูลสรุป Dashboard ในอดีตเพี้ยนตามไปด้วย (เช่น วันนี้เปลี่ยน Team2 เป็น Team1 ทำให้ประวัติเมื่อวานกลายเป็น Team1 ด้วย)
  2. เกิด Error "Failed to save settings" บนหน้าจอ UI ตอนที่บันทึกค่า Team Settings สำเร็จแล้ว (False Positive)
  3. ผู้ใช้ต้องการให้แก้ไขข้อมูลในอดีตของแผนก "Toolbox - Team2 D-DL" และ "Toolbox - Team2 M-DL" ให้กลับไปเป็น `TEAM 2` เหมือนเดิมก่อนหน้าที่จะมีการสลับกลุ่มวันนี้
- **สิ่งที่แก้ไขไป (What)**:
  1. เพิ่มคอลัมน์ `actual_hc_group` ในตาราง `dbo.MANPOWER_DAILY_LOGS` เพื่อแช่แข็ง (Snapshot) ค่าของกลุ่มคน ณ วันนั้นๆ
  2. แก้ไข SQL Function `fn_GetManpowerSummary_V2` ให้ใช้ `actual_hc_group` เป็นหลัก หากไม่มีให้ใช้ `MANPOWER_TEAM_SETTINGS`
  3. อัปเดต `sync_from_api.php` และ `api_daily_operations.php` ให้ดึงค่า `hc_group` ปัจจุบัน และแช่แข็งมันลงในคอลัมน์ใหม่ทุกครั้งที่รับเวลาสแกนหรือบันทึก
  4. รัน SQL สคริปต์แก้ไขข้อมูลประวัติศาสตร์ทั้งหมด (74,626 rows) ให้จับคู่ตามการตั้งค่าปัจจุบัน และตั้งค่าให้ `"Toolbox - Team2 D-DL"` กับ `"Toolbox - Team2 M-DL"` ของวันที่ 2026-08-24 (เมื่อวาน) ถูกนับเป็น `TEAM 1` ตามคำสั่งของ Admin
  5. แก้ไขโค้ด Javascript ใน `manpower_ui.js` ที่พยายามปิด Modal ผิด ID จนเกิด TypeError
  6. จัดการดึงรายชื่อและดำเนินการอัปเดตสถานะ (ให้ออก / Inactive) นักศึกษาฝึกงานจำนวน 39 คน จากอดีตกลุ่ม TEAM 2 ที่ไม่ได้ทำการสแกนหน้าหรือมีสถานะเป็น ABSENT เมื่อวันที่ 2026-08-24 ตามคำสั่งของ Admin พร้อมปรับ `resign_date` เป็นวันที่ `2026-08-24` และลบข้อมูลการลงเวลาของวันที่ 24-25 ออกจาก `MANPOWER_DAILY_LOGS` เพื่อไม่ให้ถูกนับรวมใน Headcount อีกต่อไป
- **ผลลัพธ์ (Result)**:
  1. หน้า Dashboard ประวัติศาสตร์แสดงผลถูกต้อง ไม่ถูกแก้ไขย้อนหลังเมื่อสลับทีมปัจจุบัน
  2. แอดมินเซฟ Team Settings แล้วขึ้น Success เด้งถูกต้อง ไม่โชว์ Error สีแดงปลอมอีกต่อไป
- **ไฟล์ที่แก้ไข (Files Modified)**: `manpower_ui.js`, `sync_from_api.php`, `api_daily_operations.php` (อัปโหลดขึ้น FTP เรียบร้อยแล้ว)
- **DB ที่เปลี่ยนแปลง**: `dbo.MANPOWER_DAILY_LOGS` (ALTER ADD COLUMN), `dbo.fn_GetManpowerSummary_V2` (ALTER FUNCTION)
