## 🔴 Currently Working On

---

## ✅ Completed

### 👨‍💻 Agent Activity (08:30 - 09:40)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**: 
  1. พบปัญหา "ผลัดดึกที่ขึ้นเช้า" ระบบโยนเวลาเช้าทิ้ง ทำให้ตรวจจับกะผิดไม่ได้
  2. ข้อมูลพนักงานที่ยังไม่เข้างานเลย (ทั้งเช้าและดึก) ไม่ถูกดึงขึ้นมาเตือนจนกว่าจะหมดวัน
  3. แอดมินต้องการปุ่ม "ยืนยัน (Confirm)" สำหรับเคสที่ถูกต้องแล้วให้หายไปจากหน้าจอแจ้งเตือน
  4. เพิ่มฟีเจอร์ Batch สำหรับการ Confirm ทีละหลายคน
- **สิ่งที่แก้ไขไป (What)**: 
  - เพิ่มระบบ Fallback (ช้อนเวลาตกหล่น) ในระบบดึงข้อมูลจาก API เพื่อตรวจจับเวลาคนกะดึกมาสแกนตอนเช้า
  - อัปเดต SQL Logic ให้จับคนที่ไม่สแกนเข้างานเมื่อเลยเวลา (08:30 สำหรับเช้า, 20:30 สำหรับดึก) แจ้งเตือน `MISSING_IN_SCAN`
  - เพิ่มปุ่ม และฟังก์ชัน Batch Dropdown สำหรับ "ยืนยันความถูกต้อง" (is_verified = 1) ในหน้า Shift Swap Audit
- **ผลลัพธ์ (Result)**: แอดมินสามารถจัดการความผิดปกติได้ล่วงหน้า และล้างรายการที่ตรวจเช็คแล้วออกจากคิวได้ทันที (รวมถึงทำแบบ Batch ได้)
- **ไฟล์ที่แก้ไข (Files Modified)**: `api_daily_operations.php`, `sync_from_api.php`, `manpower_ui.js`, `master_settings_modal.php` (ทั้งหมดอัพโหลดขึ้น FTP เรียบร้อย)

### 👨‍💻 Agent Update (09:48)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  1. พบ Syntax Error (Unexpected token `async`) ใน `manpower_ui.js` หลังเพิ่มโค้ด
  2. พบ PHP Warning (Undefined variable `procDate`) ส่งกลับมาเป็น HTML ทำให้ JSON พังใน `sync_from_api.php` ตอนสั่ง Sync
- **สิ่งที่แก้ไขไป (What)**:
  - แก้ไขการเว้นวรรคและการใส่เครื่องหมาย `,` ใน `manpower_ui.js`
  - เปลี่ยนตัวแปร `$procDate` เป็น `$dateStr` ใน Fallback logic ของ `sync_from_api.php`
- **ไฟล์ที่แก้ไข (Files Modified)**: `manpower_ui.js`, `sync_from_api.php` (ทั้งหมดอัพโหลดขึ้น FTP แล้ว)

---

### 🎯 Project Status Update & Next Steps (อัปเดตสถานะโครงการ)
- **เวลาอัปเดต (Time)**: 10:15
- **สรุปภาพรวม (Overview)**: 
  - 🛡️ **โมดูล Safety System (PE):** แผนงานทั้งหมด (Phase 1-3) ตั้งแต่ Hazard Report, e-LOTO, ไปจนถึง Pre-Op Checklist และ Safety Dashboard **"เสร็จสมบูรณ์ร้อยเปอร์เซ็นต์"** แล้วครับ (ยกเว้นส่วนเชื่อมต่อ IoT Sensor ที่เราตกลงกันว่าพักไว้ก่อน) ตอนนี้โมดูลนี้พร้อมใช้งานจริงแบบเต็มรูปแบบ
  - 👥 **โมดูล Manpower:** จากบันทึกงานช่วงเช้า มีการแก้บั๊กเรื่องกะดึก, เพิ่ม Batch Confirm และแก้บั๊ก Syntax Error แล้ว
- **สิ่งที่ยังเหลือ / ต้องทำต่อ (Remaining Tasks)**:
  1. **ทดสอบระบบ Manpower:** รอดูว่าลอจิกช้อนเวลาตกหล่น (Fallback) ของกะดึกที่เพิ่งอัปเดตไป ทำงานกับข้อมูลจริงบนโปรดักชั่นได้อย่างถูกต้อง และปุ่ม Batch Confirm ทำงานได้ลื่นไหล
  2. **รับมอบหมายงานใหม่ (Next Module/Feature):** เนื่องจากแผนงานเดิมของเราในส่วน Safety นั้นเคลียร์หมดแล้ว ตอนนี้ระบบกำลังรอคำสั่งจากคุณว่า อยากให้มุ่งเน้นไปที่การขยายฟีเจอร์โมดูลไหนต่อ (เช่น การทำรายงาน Report/Export, เริ่มทำโมดูล Inventory, หรือปรับปรุง UI ส่วนไหนเพิ่มเติม)

---

### 🔧 Antigravity — DB Migration & Audit Fix (10:17 - 10:32)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ตรวจพบ Critical Bug จากการ Audit โมดูล PE: `lotoAPI.php` ถูก implement ไว้ตั้งแต่ 21/08 แต่ **ไม่มีการ migrate DB** ทำให้ฟีเจอร์ E-LOTO ไม่สามารถใช้งานได้จริง และเป็นความเสี่ยงด้านความปลอดภัย
- **สิ่งที่แก้ไขไป (What)**:
  1. เพิ่ม columns `is_loto BIT DEFAULT 0` และ `loto_reason NVARCHAR(500)` ใน `PE_MACHINES`
  2. สร้างตาราง `PE_LOTO_LOGS` (log_id, machine_id, wo_id, locked_by, locked_at, unlocked_by, unlocked_at, status, reason, updated_at) พร้อม FK ไปยัง `PE_MACHINES`
  3. สร้างไฟล์ `db_changes.md` ที่ root เพื่อ track schema changes ตาม AGENTS.md protocol
- **ผลลัพธ์ (Result)**: E-LOTO สามารถใช้งานได้จริงแล้ว ทุก action (lock/unlock/status) ใน `lotoAPI.php` มี DB รองรับครบ
- **ไฟล์ที่แก้ไข (Files Modified)**: `db_changes.md` (สร้างใหม่ที่ root)
- **DB ที่เปลี่ยนแปลง**: `PE_MACHINES` (ALTER), `PE_LOTO_LOGS` (CREATE)

### 🛠️ Antigravity — LOTO Line Notify Integration (13:28 - 13:30)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ตามแผนระยะที่ 4 ใน PE ROADMAP.md ต้องการระบบส่งแจ้งเตือน (Line Notify) เมื่อมีการเปิดหรือปิด LOTO กับเครื่องจักรระดับความสำคัญสูง (Critical/High)
- **สิ่งที่แก้ไขไป (What)**:
  1. เพิ่มฟังก์ชัน `sendLineNotify()` ในไฟล์ `logger.php` (global utility) 
  2. แก้ไขไฟล์ `lotoAPI.php` ให้เช็คฟิลด์ `criticality` ของตาราง `PE_MACHINES` เมื่อมีการล็อกหรือปลดล็อก
  3. หากเป็น 'High' หรือ 'Critical' ระบบจะส่ง Notify แจ้งเตือนไปยัง Line ทันที พร้อมบอกผู้กระทำและเหตุผล
  4. เพิ่มโค้ดโครงร่างการตั้งค่า `LINE_NOTIFY_TOKEN` ในไฟล์ `.env`
  5. อัปเดตสถานะใน `ROADMAP.md`
- **ไฟล์ที่แก้ไข (Files Modified)**: `logger.php`, `lotoAPI.php`, `ROADMAP.md`
- **ผลลัพธ์ (Result)**: ผู้จัดการจะได้รับการแจ้งเตือนอัตโนมัติหากเครื่องจักรสำคัญถูกล็อค (LOTO)

### 🛠️ Antigravity — In-App Notification Center (13:45 - 13:55)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ผู้ใช้งานต้องการให้ระบบส่งแจ้งเตือน LOTO และระบบแจ้งเตือนอื่นๆ ให้อยู่ภายในแอป MES เท่านั้น เพื่อให้เป็น Single Source of Truth และหลีกเลี่ยงข้อจำกัด/ค่าใช้จ่ายของ Line Notify
- **สิ่งที่แก้ไขไป (What)**:
  1. สร้างตารางใหม่ `PE_NOTIFICATIONS` สำหรับจัดเก็บคิวแจ้งเตือน
  2. ยกเลิกการใช้ Line Notify ใน `lotoAPI.php` และเปลี่ยนมาบันทึกลง `PE_NOTIFICATIONS` แทน เมื่อมีการ Lock/Unlock เครื่องจักร
  3. สร้าง `getNotifications.php` เพื่อดึงแจ้งเตือนที่ยังค้างอยู่
  4. เพิ่มไอคอนกระดิ่ง 🔔 (Notification Bell) และ Dropdown รายการแจ้งเตือน ใน Topbar ของหน้า PE Dashboard
  5. เขียนสคริปต์ใน `peApp.js` ให้เช็คแจ้งเตือนใหม่ทุกๆ 10 วินาที หากพบแจ้งเตือนใหม่ จะเด้ง Popup (Toast) ขึ้นที่มุมจอด้านขวาบนทันที
- **ผลลัพธ์ (Result)**: ผู้ใช้งานทุกคนที่เปิดหน้า Dashboard ค้างไว้ จะเห็นการแจ้งเตือน LOTO เครื่องจักรสำคัญในเวลาแบบเกือบเรียลไทม์ผ่าน Popup และตัวเลขแจ้งเตือน โดยไม่มีค่าใช้จ่าย
- **ไฟล์ที่แก้ไข (Files Modified)**: `db_changes.md`, `lotoAPI.php`, `getNotifications.php` (สร้างใหม่), `peDashboard.php`, `peApp.js`

### 🛠️ Antigravity — Canvas Animation & GPU Optimization (14:10 - 14:27)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ผู้ใช้งานพบอาการหน้าจอกระพริบและจอดำเมื่อเปิดหน้า PE Dashboard (Task Manager แสดงการ์ดจอ Intel ทำงาน 100% และมี Error `igfxn` แครช) และพบอาการกระพริบซ้ำแม้สลับไปหน้าจอ Work Order (เกิดจากอนิเมชันรันค้าง 10 FPS ตีกับหน้าจอ G-Sync)
- **สิ่งที่แก้ไขไป (What)**:
  1. เพิ่ม `enableRetinaScaling: false` และ `imageSmoothingEnabled: false` ใน `mapBuilderModule.js` เพื่อลดภาระการประมวลผลพิกเซลของการ์ดจอออนบอร์ดลง 4 เท่า
  2. ยกเลิกการจำกัด 10 FPS (`requestAnimationFrame` throttle) เปลี่ยนไปรันที่ 60FPS ธรรมชาติ เพื่อแก้ไขอาการ G-Sync Monitor Flicker
  3. เพิ่มคำสั่ง Pause อนิเมชัน `animateConveyors` และ `animateAlerts` อัตโนมัติเมื่อผู้ใช้สลับไปแท็บอื่น 
  4. สั่งปิด Service `performance_logger.cjs` ที่ทำงานทุก 1 นาที (ซึ่งไปกระตุ้นแอนตี้ไวรัส Trend Micro) ทิ้ง
- **ผลลัพธ์ (Result)**: หน้าจอหายกระพริบ 100% และกราฟการ์ดจอออนบอร์ดลดลงมาเหลือ 5-20%
- **ไฟล์ที่แก้ไข (Files Modified)**: `mapBuilderModule.js`, `iiotModule.js`

### 🛠️ Antigravity — QMS Concession Image Attachments
- **Module:** QMS / Concession
- **Description:** Added functionality to attach 1-3 images to Concession documents. Created a new PDF page (Page 3) to render these attachments beautifully to avoid cluttering the main ISO form.
- **Files Modified:**
  - `page/QMS/api/concession_api.php`
  - `page/QMS/components/concession_list.php`
  - `page/QMS/components/concession_script.php`
  - `page/QMS/print_concession.php`
- **Database Changes:** `ALTER TABLE QMS_CONCESSION ADD attached_image_1, attached_image_2, attached_image_3;`
- **Status:** ✅ Completed

### 🛠️ Antigravity — QMS Concession PDF Print & UI Adjustments (16:20 - 16:55)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  1. ไฟล์ PDF ปรินต์ล้นหน้ากระดาษ และตารางฉีกขาดกลางบรรทัด
  2. รหัสเอกสาร (FM-QCS-006) ไม่อยู่ในจุดที่ถูกต้องสำหรับหน้าที่ 2 และหน้าอื่นๆ
  3. Modal แสดงรายละเอียดข้อความแคบไป และยังไม่สามารถเปิดดูรูปแนบผ่านเว็บได้โดยตรง
- **สิ่งที่แก้ไขไป (What)**:
  1. เพิ่ม `page-break-inside: avoid;` ลงใน PDF stylesheet เพื่อป้องกันการหั่นครึ่งบรรทัดข้ามหน้า และลดขนาดรูปให้ 3 รูปฟิตพอดีใน 1 หน้า A4 
  2. เพิ่ม Footer `position: fixed` สำหรับ Print Mode เพื่อให้รหัสเอกสารซ้ำอยู่ขวาล่างของกระดาษทุกแผ่น
  3. ปรับขนาด Modal (สร้าง/ดู) เป็น `modal-xl` และเขียน HTML/JS อัดฉีดรูปภาพเข้าไปใน View Modal
- **ผลลัพธ์ (Result)**: PDF สวยงามอ่านง่ายขึ้น รหัสเอกสารถูกต้อง รูปภาพดูผ่านเว็บได้ทันที
- **ไฟล์ที่แก้ไข (Files Modified)**: `print_concession.php`, `concession_list.php`, `concession_script.php`
### 🛠️ Antigravity — QMS QA/QC Planner Calendar Fix (15:43 - 15:46)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ผู้ใช้แจ้งว่าปฏิทินใน QA/QC Planner ไม่สามารถเลื่อนดูรายการทั้งหมดได้ในวันที่ที่มีคิวงานซ้อนกันเยอะ
- **สิ่งที่แก้ไขไป (What)**:
  1. นำระบบการจำกัดจำนวน Event ที่โชว์สูงสุด 2 อัน (`slice(0, 2)`) ออก เพื่อให้ Calendar render งานทั้งหมด
  2. เพิ่ม CSS Container ให้กับชุดอีเวนท์ในแต่ละช่องวัน (`.qa-desktop-events`) ให้มี `max-height: 140px` พร้อม `overflow-y: auto` ทำให้สามารถเลื่อน Scroll ดูงานภายในช่องวันนั้นๆ ได้โดยไม่ทำให้หน้าตาปฏิทินยืดจนพัง
- **ผลลัพธ์ (Result)**: ผู้ใช้สามารถ Scroll ภายในช่องของแต่ละวันเพื่อดูรายการคิวงานทั้งหมดได้ตามที่ต้องการ
- **ไฟล์ที่แก้ไข (Files Modified)**: `page/QMS/components/qa_schedule_script.php`, `page/QMS/components/qa_calendar.php`

### 🛠️ Antigravity — Safety IoT Integration (Node-RED) (14:55 - 15:06)
- **ปัญหา / สิ่งที่ต้องทำ (Why)**:
  ทำตามแผน Phase 4 ของ PE ROADMAP เพื่อเชื่อมต่อระบบเซนเซอร์ความปลอดภัย (Safety IoT) เข้ากับระบบ MES โดยตรงผ่าน Node-RED
- **สิ่งที่แก้ไขไป (What)**:
  1. สร้าง API ใหม่ safetySensorAPI.php สำหรับเป็น Webhook รับข้อมูล JSON แบบ Real-time จาก Node-RED (bypassing session_start)
  2. เชื่อมต่อเข้าไปจัดการ Flow ของ Node-RED และสร้างแท็บใหม่ชื่อ Safety IoT (MES)
  3. ผูกคำสั่ง Inject Hazard (เซนเซอร์จำลอง) ให้ยิง POST แจ้งเตือนความผิดปกติของเครื่องจักร (เช่น M-101 Light Curtain) มาที่ MES API โดยตรง
  4. ตั้งค่าให้ MES API อ่านสัญญาณแล้วแปลงเป็น PE_NOTIFICATIONS ให้อัตโนมัติ เพื่อให้เด้งแสดงผล Popup (Toast) ในหน้า PE Dashboard ทันที
- **ไฟล์ที่แก้ไข (Files Modified)**: safetySensorAPI.php (สร้างใหม่), Node-RED Flows
- **ผลลัพธ์ (Result)**: ผู้บริหาร/แอดมิน สามารถรับทราบการเตือนเซนเซอร์อันตรายได้ทันทีจากหน้า Dashboard โดยไม่ต้องพึ่ง Line Notify และใช้เป็นต้นแบบรับเซนเซอร์จริงในอนาคตได้ทันที
