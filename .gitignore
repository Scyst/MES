# --- Configuration Files ---
# ไม่ควรเก็บไฟล์ตั้งค่าที่มีข้อมูลสำคัญ (เช่น รหัสผ่านฐานข้อมูล) ไว้ใน Git
MES/config/config.php

# --- Third-Party Libraries ---
# ไม่ควรเก็บไลบรารีของโปรแกรมอื่นไว้ใน Git ควรติดตั้งแยกต่างหาก
MES/utils/libs/

# --- Old Versions ---
# ไม่ควรเก็บโค้ดเวอร์ชันเก่าที่ไม่ใช้งานแล้วไว้ใน Repository
oldVer/